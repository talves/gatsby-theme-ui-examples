import Layout from '../layout'
import {GatsbyLinkButton, OutsideLink} from '../components/gatsby-link.js'

export default Layout

# Hello

This page is written in <OutsideLink to={'https://theme-ui.now.sh'}>MDX</OutsideLink> and styled with <OutsideLink to={'https://mdxjs.com'}>Theme UI</OutsideLink>.

<GatsbyLinkButton to={'/a'}>Theme A (deep)</GatsbyLinkButton>
<GatsbyLinkButton to={'/b'}>Theme B (swiss)</GatsbyLinkButton>
