import { Layout } from '../components/Layouts'
import {GatsbyLinkButton, GatsbyLink, OutsideLink} from '@talves/gatsby-theme-components-core'

export default Layout

# Hello

This page is written in <OutsideLink sx={{fontSize: '1.25em'}} to={'https://mdxjs.com'}>MDX</OutsideLink> and styled with <OutsideLink sx={{fontSize: '1.25em'}} to={'https://theme-ui.now.sh'}>Theme UI</OutsideLink>. 

The page is now using the new presets plugin.

The site is now using a provider plugin.

The site is now using a layout plugin and we added a test page plugin to see how to use the provider plugin. This also shows how we can shadow our layout from the site and all plugins end up using it.

The site is now using a components plugin for our common core theme components.

-----------------  

## What will we be doing here?

  * Start with a simplified theme setup
  * Restructure our theme into basic re-usable theme plugins
  
-----------------  

## Instructions by Branch


<GatsbyLink to={'/simplified'}>01 - Simplified</GatsbyLink><br />
<GatsbyLink to={'/presets'}>02 - Presets</GatsbyLink><br />
<GatsbyLink to={'/provider'}>03 - Provider</GatsbyLink><br />
<GatsbyLink to={'/layout'}>04 - Layout</GatsbyLink><br />
<GatsbyLink to={'/components'}>05 - Components</GatsbyLink>

-----------------

## Themes

<GatsbyLinkButton to={'/test'} sx={{ margin: '0.25em 0'}}>Test Theme</GatsbyLinkButton><br />
<GatsbyLinkButton to={'/a'} sx={{ margin: '0.25em 0'}}>Theme A (deep)</GatsbyLinkButton><br />
<GatsbyLinkButton to={'/b'} sx={{ margin: '0.25em 0'}}>Theme B (swiss)</GatsbyLinkButton>
